services:
  ci:
    build:
      context: .
      target: codex-ci
    container_name: ${CI_CONTAINER}
    entrypoint:
      - sleep
      - infinity
    environment:
      - PYTHONUNBUFFERED=1
    image: ${CI_IMAGE}
    volumes:
      - ./test-results:/app/test-results
      - ./dist:/app/dist
